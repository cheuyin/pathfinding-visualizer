// Centralised constants for UI and animation timings

// Grid
export const CELL_SIZE_PX = 25;

// Animation delays (milliseconds)
export const VISITED_NODE_DELAY_MS = 2;
export const PATH_NODE_DELAY_MS = 20;
export const MAZE_WALL_DELAY_MS = 1;
